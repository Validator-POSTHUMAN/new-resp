# RESP Proposals

This directory will contain DAO DAO proposal drafts and TokenFactory action
payloads.

No proposal payload should be treated as ready until it is reviewed against the
final snapshot and live Cosmos Hub TokenFactory params.

